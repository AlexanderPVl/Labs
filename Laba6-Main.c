#include <stdio.h>
#include <string.h>
#include "Structures.h"
#include "Defining.h"

TV* Task(TVArray tva, string city)
{
	TVArray TV_arr = { NULL, 0, 0 };
#if ImportArray == 1
	Imp_Array();
#else
	Enter_Array();
#endif
	printf("That's it");

	return 0;
}

int main()
{
	Task()



	//Enter_TVArr(Tv, size);
	return 1;
}
